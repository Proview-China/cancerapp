## 1. 实现
- [x] 1.1 分屏与手柄：在左预览区与右分析区之间新增可拖拽手柄（role="separator"，高度≥容器 50%），支持鼠标拖拽与键盘左右箭头微调；宽度变化遵循 clamp。
- [x] 1.2 快速导入：合并“导入影像/导入文字”为“快速导入”，统一排版与交互；面板**仅保留两项**“图像”“文字”，替换相关文案与按钮。
- [x] 1.3 侧栏保护：将侧栏最小宽度提升至新的阈值（≈420px），设置最大宽度（建议 640–720px 区间），顶置与滚动区域宽度一致；滚动条固定在最右侧且不影响手柄点击。
- [x] 1.4 文案替换/移除：将“📷/✍️”替换为“图像/文字”，移除“未设置显示名称”。
- [x] 1.5 病例分组与按钮策略：
  - 单病例内展示两个框：“图像”“文字”，各自列表集中；
  - 两框按钮组（右侧）为“新增”“展开/收起”，无计数；
  - 主病例仅“编辑”“删除”“展开/收起”，且编辑/删除仅作用病例名称。
- [x] 1.6 图片缩略展示：在缩略容器内按原图等比缩放（保持宽高比、完整可见、居中、不裁剪），确保在框体中正常比例显示；典型长宽比样例均正确。
- [x] 1.7 主题统一：统一预览标题框颜色与文字预览主题配色，符合池塘风格与可读性（对比度）。
- [x] 1.8 卡片/列表排版收尾：统一标题/摘要字号行高、摘要 2 行截断、操作区右对齐与固定间距、hover/active 阴影微调。

## 2. 检查
- [x] 2.1 DevTools/Playwright MCP：
  - 打开 `http://localhost:5173`（或 Electron 指向的 Vite 服务）
  - 手柄：可见、可聚焦；拖拽与键盘均可调整左右宽度；命中区域不与滚动条冲突
  - 侧栏：`min-width` 新阈值生效（<420px 不再压缩）；顶置/滚动宽度一致；滚动条在最右且不影响手柄；所有内容正常显示（无错位/重叠）
  - 分组：病例内“图像/文字”两框存在且按钮为“新增”“展开/收起”，主框仅“编辑/删除/展开收起”；无“未设置显示名称”与 Emoji，快速导入仅两项
  - 缩略：在缩略容器内按比例完整显示，无裁剪与变形
  - 主题：标题框与文字预览颜色统一
  - 产出 snapshot 并记录关键 CSS 取值
- [x] 2.2 前端构建：`npm run build`

## 3. 文档
- [x] 3.1 在 `frontend/README.md` 更新 UI 细节规范：分屏与手柄、侧栏阈值与一致性、快速导入入口、病例分组策略、缩略图尺寸规则、主题与排版统一。
