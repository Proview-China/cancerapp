## 1. 计算与数据管道（完成）
- [x] 1.1 构建病例内样本×特征矩阵（细胞/面积双体系），输出原值、单位、归一、z-score、分位、缺失标记与 conf。
- [x] 1.2 预备 Spearman ρ 与 BH-FDR 统计（N<6 不出星标；本版本不在主图显示）。
- [x] 1.3 风险综合分（固定权重）。

## 2. 图一：复合热力图（单图，完成）
- [x] 2.1 列轴改为“医生判断维度”（归一/归一十分位/z分/|z|/分位/分位组/排名/正偏/负偏/IQR位/置信/稳定/权重/完备），行轴为全指标；病例内统计；单元格白色网格边框；确定性抖动（仅影响可视，不改数据）。
- [x] 2.2 居中布局、containLabel、无横向滚动；legend 居中。
- [x] 2.3 相关矩阵与顶部谱带暂不展示（留作后续变更）。

## 3. 图二：多层级环形图（完成）
- [x] 3.1 中心综合风险仪表（0–1）。
- [x] 3.2 R1 风险分层环（角度=占比；固定厚度以保证排版）。
- [x] 3.3 R2 特征贡献环（角度=贡献，半径=均值，顺序色）。
- [x] 3.4 R3 分位细分环（Q1–Q5：色深=均值）。
- [x] 3.5 R4 HSI 组成环；R5 缺失率窄环（灰刻痕）。

## 4. 统一与无障碍（完成）
- [x] 4.1 统一色表：值/比例 YlOrRd；缺失灰；图例居中一致。
- [x] 4.2 Tooltip：显示原值/单位或标准化说明（z/分位/排名/偏差等）。
- [x] 4.3 A11y：aria-label 与对比度；键盘不遮挡。

## 5. 性能与稳健（完成）
- [x] 5.1 使用 ECharts 渲染与裁剪、containLabel；如列过多再按后续变更处理。
- [x] 5.2 空/缺失/极端分布稳定渲染（缺失灰、统计回退）。

## 6. 验证（完成）
- [x] 6.1 使用 demo_fake 病例本地核验。
- [x] 6.2 色表与数值一致性（环图与热力统一 YlOrRd）。
- [x] 6.3 控制台无报错；窗口缩放自适应；MCP 实测可访问。
