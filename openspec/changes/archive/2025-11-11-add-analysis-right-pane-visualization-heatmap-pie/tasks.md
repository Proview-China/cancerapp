# Tasks — add-analysis-right-pane-visualization-heatmap-pie

> 仅规划（不改代码）；以下为后续实现清单与验收点。

## 清单（按序）
- [x] 1) UI：右半区标题栏加入二级切换（“文字总结｜图像总结”），复用现有四页切换样式与 A11y 语义（tablist/tab）。
- [x] 2) 逻辑：在右半区新增 `rightMode: 'ai' | 'viz'` 状态管理；默认 `'ai'`；点击切换与键盘操作更新。
- [x] 3) 标题：统一两模式标题文案；“图像总结”模式下显示“新建对话”，点击时切回 `'ai'` 并聚焦输入框。
- [x] 4) 数据：调用已有 `fetchSampleAnalysis(caseId, sampleId)` 获取 `TissueAnalysis`；缺失则提供演示常量兜底（不依赖后端变更）。
- [x] 5) 图表库选择：对比 ECharts/AntV；优先选用 ECharts（热力图/扇形图原生支持、生态成熟、可定制样式）。
- [x] 6) 扇形图：基于标准化后的 4 指标（`ratio`/`mean_density`/`h_score`/`irs`）按阈值划分高/中/低，统计占比并渲染环形图（含 Hover）。
- [x] 7) 热力图：渲染单列矩阵（行=4 指标，列=当前患者），颜色渐变（黄→橙→红），显示数值 Tooltip。
- [x] 8) 样式与响应式：图表容器最小高 320px，自适应右半区；沿用现有主题；无溢出滚动。
- [x] 9) A11y：二级切换支持键盘（左右/Enter/Space）；图表容器带 `aria-label`；状态切换无布局抖动。
- [x] 10) 验证：本地运行 Electron；切换与图表渲染稳定，无控制台错误；窗口大小变化仍然可读。

## 验收
- 二级切换存在且可键盘操作；默认进入 AI 对话模式。
- “图像总结”视图包含两图：
  - 扇形图：展示高/中/低占比（基于 4 指标阈值分层）；Hover 显示百分比。
  - 热力图：4 行×1 列，颜色连续渐变；Hover 显示指标名与归一化值。
- “新建对话”在“图像总结”模式可见且可点击，点击后切回 AI 对话并聚焦输入。
- 缩放窗口与切换病例/样本时，图表稳定刷新且无报错。
